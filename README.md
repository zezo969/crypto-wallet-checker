# Crypto Wallet Checker

A simple Python tool that checks the basic format of an EVM wallet address.

## Features

- Checks if the address starts with 0x
- Checks the address length
- Returns a validation message

## Requirements

Python 3


## Usage

Run:

python wallet_checker.py
